# AI Knowledge Bot

A knowledge-based AI assistant for answering questions
about codebases using vector search and LLM integration.

## Quick Start

1. Install dependencies: `pip install -e .`
2. Set DATABASE_URL environment variable
3. Run: `python -m ai_knowledge_bot`

## Configuration

Configuration is managed via environment variables and
a YAML config file. See config.yaml for details.
