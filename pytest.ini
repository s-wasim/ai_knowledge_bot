[pytest]
testpaths = tests
markers =
    integration: needs a live Postgres with pgvector and pg_trgm
    eval: end-to-end retrieval quality; needs Postgres, the embedding model, and ANTHROPIC_API_KEY
filterwarnings =
    ignore::DeprecationWarning
